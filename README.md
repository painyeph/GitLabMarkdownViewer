# GitLab Mardown Viewer

* Renders markdown files in GitLab style.
* Math formulas are supported.
* Both Firefox (including Android version) and Chrome are Supported.
* Only UTF-8 encoded markdown files are supported.
